[project]
name = "agripredict-ai"
version = "1.0.0"
description = "AgriPredict AI"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
]

[tool.vercel]
entrypoint = "backend.main:app"
